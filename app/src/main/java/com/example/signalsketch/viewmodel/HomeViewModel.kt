package com.example.signalsketch.viewmodel

import androidx.lifecycle.ViewModel

class HomeViewModel : ViewModel() {
    val subtitle: String = "Choose a workflow to start scanning, mapping, or reviewing saved sessions."
}
